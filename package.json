{
  "name": "snip-bundle",
  "version": "1.0.0",
  "description": "Snip — self-contained production bundle (backend + frontend + CLI)",
  "scripts": {
    "start": "bun server.js"
  },
  "engines": {
    "bun": ">=1"
  }
}
